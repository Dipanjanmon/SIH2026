apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - postgres.yaml
  - ai-service.yaml
  - backend.yaml
  - frontend.yaml

configMapGenerator:
  - name: postgres-init-sql
    files:
      - init.sql=init.sql
